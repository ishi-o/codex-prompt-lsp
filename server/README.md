# codex-prompt-lsp

Language server for Codex prompt completions.

```sh
npx codex-prompt-lsp --stdio
```

The server provides slash-command, skill, plugin, and file completions with
server-side fuzzy filtering. Editor clients are available in the
`codex-prompt-lsp` repository.
